package in.MeenaCoding.Lambda;

public class TestingLambda {
    public static void main(String[] args) {

        // s -> System.out.println(s);
//        (a, b) -> {int sum = a + b;
//            System.out.println(sum);}
//    }

    }

    public int sum ( int a, int b) {
        int sum = a + b;
        return sum;    //normally we create this type of function and return them but with lambda we can directly that in that main area.

    }
}