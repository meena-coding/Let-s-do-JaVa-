package in.aryancoding.Abstraction;

public class TestAbstraction {
     //Vehicle veh = new Vehicle(2);   //one cannot make object of abstracted class but just extend them as inheritance to other class

    car maruti = new car();

}
